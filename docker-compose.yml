services:
  docs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ccbug-docs
    restart: unless-stopped
    ports:
      - "${DOCS_BIND_ADDRESS:-127.0.0.1}:${DOCS_PORT:-12350}:80"
